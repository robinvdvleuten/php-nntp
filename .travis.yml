language: php

php:
  - hhvm
  - 5.3
  - 5.4
  - 5.5

matrix:
    allow_failures:
        - php: hhvm

before_script:
  - composer self-update
  - composer install --dev --prefer-source --no-interaction

script:
  - ./tests/server.php &
  - php bin/phpunit
